FROM php:8.1-fpm
# Текущая директория выполнения команд. По-умолчанию это /var/www/html и если такой директории не существует, то скрипт создает ее.
# Поэтому переназначаем на /var/www, которая у нас уже есть.
WORKDIR /var/www